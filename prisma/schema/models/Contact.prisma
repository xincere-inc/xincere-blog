model Contact {
  id            Int       @id @default(autoincrement())
  companyName   String
  contactName   String
  email         String
  phone         String
  inquiry       String
  privacyPolicy Boolean
  createdAt     DateTime  @default(now()) @map("created_at")

  @@map("contacts")
}